# SLog 版本

## v1.0.0（可回退的稳定版）

半年一张表的航海日志：一周 / 一月 / 半年视图，颜色目录，格子写正文、小标题、P3、分类条目和一天最多 12 张图。自动保存、手动确认保存、导出 JSON、WebDAV 增量备份。导入默认只补空、不覆盖已写的日子。访客试用当次有效。

## v1.1.0（试验：待办）

格子可切三种内容：只看日志、只看待办、两者一起。填写页在正文和照片下加待办（默认两行，可追加）。备份 JSON 带 `schemaVersion`，旧备份没有待办也能导入。
